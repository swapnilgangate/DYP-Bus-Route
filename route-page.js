const routeId = window.routeId || new URLSearchParams(window.location.search).get('route');
const route = routeCatalog[routeId] || routeCatalog['yadrav-fata'];
const [startName, , startFee, startTime] = route.stops[0];
const [, , , arrivalTime] = route.stops.at(-1);
const driverInitials = route.driver.split(' ').map((part) => part[0]).join('');
const formattedPhone = /^\d+$/.test(route.phone) ? `${route.phone.slice(0, 5)} ${route.phone.slice(5)}` : route.phone;
document.title = `DY Patil Polytechnic | ${route.name} Route`;
document.querySelector('#route-page').innerHTML = `
  <section class="hero" aria-labelledby="route-title"><div class="hero-topline"><div class="brand"><span class="brand-mark">D</span><span>Dr. D. Y. Patil Polytechnic</span></div><div style="display:flex;align-items:center;gap:.75rem"><a href="index.html" style="display:flex;align-items:center;gap:.35rem;background:rgba(255,255,255,.15);border:1px solid rgba(255,255,255,.25);color:#fff;text-decoration:none;font-size:.78rem;font-weight:600;padding:.3rem .85rem;border-radius:999px;transition:background .2s" aria-label="Back to all routes">← All Routes</a><div class="status"><span></span> Service active</div></div></div><div class="hero-content"><div><p class="eyebrow">Student transport service</p><h1 id="route-title">${route.name} <span>to</span> Kasba Bawda</h1><p class="route-subtitle">Route ${route.no} &nbsp;•&nbsp; Morning pickup schedule</p></div><div class="arrival-card"><span>Campus arrival</span><strong>${arrivalTime.replace(' AM','')} <small>AM</small></strong><p>DYPCET, Kasba Bawda</p></div></div><div class="route-visual" aria-label="Animated bus travelling from ${route.name} to DYPCET"><div class="road-line"></div><div class="route-progress"></div><div class="stop-popup" aria-live="polite"><span class="popup-label">Now at</span><strong id="animated-stop-name">${startName}</strong><div><span id="animated-stop-time">${startTime}</span><b id="animated-stop-fee">₹${startFee}</b></div></div><div class="route-label label-start">${route.name}</div><div class="route-label label-end">DYPCET</div><div class="bus-wrap"><div class="bus" aria-label="Dr D Y Patil Polytechnic Kasba Bawda Kolhapur bus"><div class="bus-roof-light"></div><div class="bus-windows"><i></i><i></i><i></i><i></i><i></i></div><div class="bus-name">DR D Y PATIL POLYTECHNIC</div><div class="bus-place">KASBA BAWDA, KOLHAPUR</div><div class="bus-front"></div><b class="wheel wheel-one"></b><b class="wheel wheel-two"></b></div></div></div></section><section class="quick-info" aria-label="Route details"><div><span class="icon">⌖</span><p>Starting point<strong>${startName}</strong></p></div><div><span class="icon">◷</span><p>Campus arrival<strong>${arrivalTime}</strong></p></div><div><span class="icon">◉</span><p>Total stops<strong>${route.stops.length} stops</strong></p></div></section><section class="content-grid"><section class="schedule" aria-labelledby="schedule-title"><div class="section-heading"><div><p class="eyebrow">Route timetable</p><h2 id="schedule-title">Pickup schedule</h2></div><div class="legend"><i></i> Current route</div></div><div class="table-wrap"><table><thead><tr><th>Stop</th><th>Location</th><th>Fee</th><th>Pickup</th></tr></thead><tbody>${route.stops.map(([english, marathi, fee, time], index) => `<tr class="${index === route.stops.length - 1 ? 'final-stop' : ''}"><td>${String(index + 1).padStart(2, '0')}</td><td><span class="stop-name">${english}</span>${marathi ? `<span class="marathi">${marathi}</span>` : ''}</td><td class="fee">${fee === '—' ? fee : `₹${fee}`}</td><td class="time">${time}</td></tr>`).join('')}</tbody></table></div></section><aside class="sidebar"><section class="driver-card" aria-labelledby="driver-title"><p class="eyebrow">Your driver</p><div class="driver-profile"><div class="avatar">${driverInitials}</div><div><h2 id="driver-title">${route.driver}</h2><p>Route ${route.no} driver</p></div></div><a class="contact" href="tel:${route.phone}"><span>Call driver</span><strong>${formattedPhone}</strong></a></section><section class="vehicle-card"><p class="eyebrow">Assigned vehicle</p><div class="vehicle-id"><span>MH</span><strong>${route.bus.replace('MH09-', '09-')}</strong></div><p class="vehicle-note">Please arrive at your pickup point 5 minutes early.</p></section></aside></section><footer>Dr. D. Y. Patil Polytechnic, Kasba Bawda, Kolhapur</footer>`;
const routeVisual = document.querySelector('.route-visual');
const stopPopup = document.querySelector('.stop-popup');
const animatedStopName = document.querySelector('#animated-stop-name');
const animatedStopTime = document.querySelector('#animated-stop-time');
const animatedStopFee = document.querySelector('#animated-stop-fee');
const positions = route.stops.map((_, index) => 7 + (index * 81) / (route.stops.length - 1));
const wait = (duration) => new Promise((resolve) => setTimeout(resolve, duration));
async function runRoute() { while (true) { for (let index = 0; index < route.stops.length; index += 1) { const [english, , fee, time] = route.stops[index]; stopPopup.classList.remove('visible'); routeVisual.style.setProperty('--route-position', positions[index]); await wait(900); animatedStopName.textContent = english; animatedStopTime.textContent = time; animatedStopFee.textContent = fee === '—' ? 'Campus' : `₹${fee}`; stopPopup.classList.add('visible'); await wait(1800); } stopPopup.classList.remove('visible'); await wait(900); } }
if (!window.matchMedia('(prefers-reduced-motion: reduce)').matches) runRoute();
